# Godot 项目开发日志

> **文档说明**：本文档记录项目开发过程中的重要修改，用于每日回顾和上下文衔接。
>
> **记录原则**：
> - 只记录重要的新增、优化、删除，不记录 BUG 修复等琐碎内容
> - 每日总结置于最前方，便于快速阅读
> - 每条记录标注日期和时间

---

## 📋 最近更新
 
### 📅 2026-05-08 开发总结

**今日核心进展**：
- 新增独立通用合成台界面，用于后续装备合成与地图合成调试。
- 合成台暂不接入现有背包、HUD 或场景入口，仅保留外部数据、规则、预览与合成执行接口。
- 调整合成台背包区域布局，压缩分类页签占比并限制物品悬浮说明尺寸。
- 新增地图元素 CSV 配置，并让合成台背包优先读取 Items/MapElements 配置数据。
- 补齐并扩展 `MapElements` 页签内容，按地图基底、地形、水域、地点、植物、矿物、食物、生物、危险分类排序。
- 在合成台内新增地图元素卡包商店模式，支持金币显示、购买扣款、开包展示和地图元素入背包。
- 清理合成台初始调试背包物品，背包只保留开包后获得的物品。
- 优化物品悬浮详情面板的自适应尺寸和边界定位规则。
- 调整卡包售空表现，售空商品保留原位并显示已售空。

**主要修改模块**：
- 通用合成台 UI：新增台面基底槽、材料槽、结果属性预览、背包格子、分类页签与悬浮说明。
- 地图元素商店 UI：新增卡包商品位、卡包概率悬浮说明、金币栏与开包弹层。

**待解决问题**：
- 后续需要接入真实背包数据、合成规则、language 表和正式入口。

---

### 📅 2026-04-21 开发总结

**今日核心进展**：
- 完成敌人“听觉-疑惑-索敌-脱锁”链路的工程化增强，统一“最后发声点”语义并加入地图可视化标记。
- 完成追踪导航防卡死能力升级：追击中可在障碍阻挡时触发自适应绕行，降低被大型 WorldEntity 卡住概率。
- 清理 GameHUD 中废弃的 NewDayButton/HeightInput/WidthInput 组件，保持 UI 结构整洁。

**主要修改模块**：
- 敌人 AI 模块：疑惑点语义统一、脱锁回疑惑、头顶状态提示、最后发声点地图标记、追踪防卡住绕行。
- HUD 模块：移除废弃按钮与输入框组件，保留现有饥饿/噪音展示链路。
- 听觉组件模块：配合 Enemy 接收链路，确保最后发声点可持续更新并用于行为决策。

**待解决问题**：
- 继续观察大体积障碍场景下绕行参数（最大探测距离、绕行时长）是否需要按敌人类型细分。

---

## 📝 详细修改记录

### 2026-05-08

#### [16:01] 通用合成台界面 - 新增
**需求描述**：新增一个独立通用合成界面，用于后续装备合成与地图合成，先完成界面调试与接口预留，不接入现有功能。

**开发内容**：
- 新增 `CraftingWorkbenchUI` 独立场景，页面分为上方合成台面与下方背包栏位。
- 台面包含左侧基底槽、中间材料槽、右侧成品属性预览，并在台面下沿提供合成按钮与状态提示。
- 背包栏位支持分类页签、格子显示、点击选择基底/材料、点击台面卸回背包。
- 实现悬浮说明面板，并根据格子所在屏幕象限决定说明面板朝向，避免超出屏幕。
- 预留 `set_inventory_items()`、`set_rule_interfaces()`、`craft_requested`、`workbench_changed` 等外部接入接口。

**开发方式**：
- 采用独立场景 + 单一 UI 控制脚本实现，内置调试数据用于单场景验证，不连接 `GameHUD`、`HomeScene`、`GameDataManager` 或真实合成系统。

**涉及文件**：
- `scenes/HUD_Scene/CraftingWorkbenchUI.tscn`（新增）
- `script/HUD/CraftingWorkbenchUI.gd`（新增）

---

#### [16:18] 通用合成台界面 - 优化
**需求描述**：缩小物品悬浮说明与背包页签区域，修正合成后装备卸回背包时丢失合成结果的问题。

**开发内容**：
- 限制 `TooltipPanel` 的最大宽高、文字长度和显示行数，并把悬浮位置约束在背包面板矩形范围内。
- 压缩背包分类页签按钮高度、字体和间距，让背包格子区域获得更多纵向空间。
- 在 `CraftButtonRow` 外新增 `CraftButtonMargin` 容器，方便后续单独调整合成按钮行的上下留白。
- 合成结果生成独立 `instance_id`，背包内部优先按实例标识匹配物品，避免 `旧木剑 +2` 卸下后被合并回原始旧木剑。

**开发方式**：
- 仅调整独立合成台场景和脚本，不接入其他功能，不影响现有 HUD 与背包界面。

**涉及文件**：
- `scenes/HUD_Scene/CraftingWorkbenchUI.tscn`（优化）
- `script/HUD/CraftingWorkbenchUI.gd`（优化）

---

#### [16:35] 地图元素配置表 - 新增
**需求描述**：新增地图元素配置表，让配置后的地图元素能够显示在通用合成台背包中。

**开发内容**：
- 新增 `script/Data/CSVs/MapElements.txt`，包含字段表头、中文说明行和地图基底/地图元素示例数据。
- 扩展 `excel_manager.py`，支持 `MapElements` 页签与 `MapElements.txt` 之间的导出同步。
- 更新合成台数据读取逻辑，优先读取 `Items.txt` 和 `MapElements.txt`，并将普通资源、装备、地图基底、地图元素转换为背包格子数据。
- 更新配置工具说明文档，修正 Excel 实际路径为 `SoData/GameData.xlsx`，并补充 `MapElements` 页签用法。

**开发方式**：
- 先通过 CSV 打通独立合成台的数据读取链路；`MapElements` Excel 页签已在后续配置优化中补写。

**涉及文件**：
- `script/Data/CSVs/MapElements.txt`（新增）
- `script/Tools/excel_manager.py`（优化）
- `script/Tools/README.md`（优化）
- `script/HUD/CraftingWorkbenchUI.gd`（优化）

---

#### [16:52] 地图元素配置表 - 优化
**需求描述**：根据已补充的中文地图元素名称，补齐对应的英文 ID、分类、品质、说明、数量和属性，并扩展更多符合设定的地图元素。

**开发内容**：
- 将用户新增的地图元素中文名同步为对应配置数据，修正重复 `echo_signal` 和旧 `map_element` 分类残留。
- 细分地图元素分类为 `terrain`、`water`、`site`、`plant`、`mineral`、`food`、`creature`、`hazard`，并按分类重新排序。
- 基于现有平原探索、生存资源、危险遭遇与异常地点设定，新增 33 个地图元素配置。
- 同步导出 `MapElements` 页签到 `script/Data/CSVs/MapElements.txt`，并更新配置工具说明与初始化模板。

**涉及文件**：
- `SoData/GameData.xlsx`（优化）
- `script/Data/CSVs/MapElements.txt`（优化）
- `script/Tools/excel_manager.py`（优化）
- `script/Tools/README.md`（优化）

---

#### [17:29] 地图元素卡包商店 - 新增
**需求描述**：在合成台界面内新增购买地图元素入口，通过金币购买卡包，并从卡包中随机开出地图元素。

**开发内容**：
- 在合成台顶部新增 `购买地图元素` / `前往合成表` 切换按钮，并常驻显示 `金币：xxxx`。
- 合成模式显示原台面；商店模式将上半部分切换为横向卡包商品位，下方背包栏位保持不变。
- 按 `MapElements` 的地图元素分类自动生成卡包，售空后商品位刷新并向前补位。
- 卡包悬浮说明显示同分类地图元素的抽取概率；未首次抽中的元素显示为 `？？？ + 概率`。
- 点击购买时允许金币扣为负数，并播放卡包位置飞向金币栏的扣款数字动效。
- 购买后弹出开包界面，居中显示开出的地图元素格子，关闭弹层后把开出的元素加入背包。
- 在 `MapElements` 页签和 CSV 中新增 `pack_probability` 字段，用于配置同分类卡包内的抽取权重。

**开发方式**：
- 仅与独立 `CraftingWorkbenchUI` 关联，不接入正式经济、回合债务、真实背包或场景入口；正式接入时可用外部金币与背包接口替换当前调试状态。

**涉及文件**：
- `scenes/HUD_Scene/CraftingWorkbenchUI.tscn`（优化）
- `script/HUD/CraftingWorkbenchUI.gd`（优化）
- `SoData/GameData.xlsx`（优化）
- `script/Data/CSVs/MapElements.txt`（优化）
- `script/Tools/excel_manager.py`（优化）
- `script/Tools/README.md`（优化）

---

#### [17:32] 合成台背包来源 - 优化
**需求描述**：移除之前用于调试而自动加入背包的物品，让背包只保留通过卡包开出的地图元素。

**开发内容**：
- 将卡包商品数据源与背包物品数据源拆开，`MapElements` 只用于生成卡包，不再自动灌入背包。
- 合成台初始化时背包为空；开包界面关闭后，才会把本次开出的元素加入背包。
- 删除旧的 `use_debug_items` 调试开关、`_make_debug_items()` 调试物品和旧的 Items 自动载入链路。

**涉及文件**：
- `script/HUD/CraftingWorkbenchUI.gd`（优化）

---

#### [17:57] 物品悬浮详情面板 - 优化
**需求描述**：让物品详情面板根据文本长度自动缩放，并调整开包界面与背包界面的展开方向和越界处理。

**开发内容**：
- 根据物品名称、类型、说明、属性文本测算详情面板宽高，并限制在最大尺寸内。
- 开包界面中查看物品时，详情面板默认向右下展开；仅在超出屏幕时才改向左或向上避让。
- 背包界面中查看物品时，如果详情面板超出屏幕下边缘，则整体向上位移直到不超过屏幕。

**涉及文件**：
- `script/HUD/CraftingWorkbenchUI.gd`（优化）

---

#### [18:16] 地图元素卡包售空表现 - 优化
**需求描述**：卡包售空后保留原商品位置，不再让后续卡包向前缩进，并显示售空文案。

**开发内容**：
- 移除售空卡包从商品列表中剔除的逻辑，保留原商品位顺序。
- 售空后按钮显示 `XX卡包已售空` 并禁用购买。
- 回退卡包详情面板的强制 Label 高度逻辑，恢复为根据内容高度计算面板尺寸，不修改概率文本内容。

**涉及文件**：
- `script/HUD/CraftingWorkbenchUI.gd`（优化）

---

### 2026-04-21

#### [09:40] HUD 场景组件 - 删除
**需求描述**：移除已废弃的家园调试入口，避免 UI 冗余和误触发。

**开发内容**：
- 删除 `GameHUD` 场景中的 `NewDayButton`、`HeightInput`、`WidthInput` 三个节点。
- 全局检索确认无脚本残留引用，避免后续空路径或失效绑定。

**涉及文件**：
- `scenes/HUD_Scene/Game_Hud.tscn`（删除）

**涉及文件**：
- `script/HUD/GameHUD.gd`（检查）

---

#### [10:25] 敌人听觉与状态提示 - 优化
**需求描述**：统一敌人“听到声音后”的语义与表现，增强状态可观察性。

**开发内容**：
- 统一术语为“最后发声点”，并围绕该点组织疑惑调查逻辑。
- 新增敌人头顶三态提示：疑惑 `?`、锁定 `!`、脱锁 `...`。
- 处理脱锁回疑惑时的目标衔接，避免继续追旧疑惑点。

**开发方式**：
- 在敌人主脚本中集中管理提示状态和最后发声点数据，保留对外接口兼容。

**涉及文件**：
- `script/Character/Enemy_state/enemy.gd`（优化）
- `script/Character/Enemy_state/EnemyPatrolState.gd`（优化）
- `scenes/characterBase_scenes/Enemy_1.tscn`（修改）
- `scenes/characterBase_scenes/Enemy_2.tscn`（修改）

---

#### [11:10] 最后发声点可视化 - 优化
**需求描述**：将最后发声点改为地图标记展示，替代坐标文本展示。

**开发内容**：
- 移除头顶坐标文本方案，改为在 `_draw()` 中绘制最后发声点标记。
- 新增标记开关、颜色、半径等可调参数，便于按关卡调试。
- 让索敌过程中最后发声点持续更新，保证脱锁后的调查目标是最新点位。

**开发方式**：
- 采用局部绘制（敌人本地坐标）实现轻量可视化，减少场景节点膨胀。

**涉及文件**：
- `script/Character/Enemy_state/enemy.gd`（优化）
- `scenes/characterBase_scenes/Enemy_1.tscn`（修改）
- `scenes/characterBase_scenes/Enemy_2.tscn`（修改）

---

#### [12:05] 敌人追踪防卡死绕行 - 优化
**需求描述**：解决敌人追踪目标时被 WorldEntity 大体积障碍阻挡后无法绕行的问题。

**开发内容**：
- 在通用导航移动中加入“卡住检测 + 绕行恢复”机制。
- 将绕行触发条件收紧为“位移不足 + 前方真实阻挡”，避免正常移动抖动。
- 增加自适应绕行：多档侧向探测距离 + 动态绕行时长，提升大障碍绕行成功率。
- 强制清仇恨时重置导航恢复器状态，避免状态残留。

**开发方式**：
- 通过导航层统一增强，不改状态机主流程；参数化导出便于 Inspector 调优。

**涉及文件**：
- `script/Character/Enemy_state/enemy.gd`（优化）

---

#### [13:05] 敌人追踪避障策略 - 优化
**需求描述**：让敌人遇到圆形障碍优先“滑行通过”，仅在双侧封堵时执行大圈绕行。

**开发内容**：
- 在导航主循环中加入前向阻挡主动探测，命中障碍时优先生成“切线滑行”速度。
- 基于障碍法线计算双侧切线候选方向，选取未被阻挡的一侧进行滑行，避免顶住抖动。
- 当切线两侧都不可行时，保留并强化“大绕行”策略，扩展多档探测倍率以支持相邻大障碍封堵场景。
- 新增滑行相关导出参数（探测距离、前向混合权重），支持按敌人类型微调表现。

**开发方式**：
- 采用“滑行优先、绕行兜底”的两级导航恢复机制，不改变现有状态机主流程。

**涉及文件**：
- `script/Character/Enemy_state/enemy.gd`（优化）

---

#### [13:35] 敌人追击卡死恢复链路 - 优化
**需求描述**：解决敌人在相邻障碍夹角中长时间卡住，且追击状态每帧覆盖绕路目标的问题。

**开发内容**：
- 在通用导航恢复器中新增“严重卡住计时器”，超过阈值后自动生成临时绕路点。
- 新增临时绕路点半径与到达阈值参数，支持小圈失败后切换更远路径。
- 追击状态改为先读取“实际追击导航目标”（优先临时绕路点），避免每帧重置成玩家位置。
- 临时绕路到达后自动退出，恢复正常最优路径追击。

**开发方式**：
- 复用现有导航层，不改状态机结构；采用“滑行 -> 绕行 -> 临时绕路点”三级恢复链路。

**涉及文件**：
- `script/Character/Enemy_state/enemy.gd`（优化）
- `script/Character/Enemy_state/EnemyChaseState.gd`（优化）

---

#### [14:10] 玩家输入锁定改造 - 优化
**需求描述**：将鼠标左键功能收敛为“仅锁定/解锁目标”，并与原攻击输入解耦。

**开发内容**：
- 断开“鼠标左键=主攻击”绑定，主攻击输入改为读取独立动作 `main_attack`。
- 保留鼠标左键点击锁定：点击敌人/物件锁定，点击空白时若已锁定则解锁。
- 调整 Shift 行为为“仅在已锁定状态下执行解锁”，避免与点击锁定职责冲突。

**开发方式**：
- 在输入中枢 `GameInputEvents` 统一切断左键攻击入口，在 `Player` 输入层集中处理锁定语义。

**涉及文件**：
- `script/Character/CharacterBase/game_input_events.gd`（优化）
- `script/Character/Player_state/player.gd`（优化）

---

#### [14:30] 点击锁定命中精度 - 优化
**需求描述**：修正敌人点击锁定判定过大问题，避免点击到非精灵区域也触发锁定。

**开发内容**：
- 在点击锁定流程中加入角色目标二次过滤，要求鼠标命中角色精灵可视矩形。
- 继续保留物理点查询作为第一层候选筛选，减少额外遍历成本。
- 通过“碰撞候选 + 精灵矩形命中”组合，降低敌人挂载大碰撞体导致的误锁定。

**开发方式**：
- 在 `Player` 层集中处理点击命中精度，不改 `CharacterBase` 通用索敌逻辑。

**涉及文件**：
- `script/Character/Player_state/player.gd`（优化）

---

#### [15:05] ERS 卡片渲染链路 - 优化
**需求描述**：将 ERS 卡片改为场景实例化渲染，确保 `TextureRect` 的缩放配置可稳定生效。

**开发内容**：
- 新增 `ERS_CardItem` 卡片预制场景，统一承载图标、名称、描述、价格四段 UI。
- 新增卡片节点脚本接口：`setup_card()` 与 `set_purchased_visual()`，用于数据绑定和购买态表现。
- `ERS_Manager` 改为通过 `PackedScene` 实例化卡片，不再使用临时 `Button.icon` 拼装方式。
- 在 `ERSLayer` 绑定 `card_item_scene`，让抽卡流程直接生成卡片实例。

**开发方式**：
- 采用“场景模板 + 数据注入”方式替代运行时纯代码拼 UI，避免布局与拉伸参数被动态结构绕开。

**涉及文件**：
- `script/HUD/ERS_Manager.gd`（优化）
- `script/HUD/ERS_CardItem.gd`（新增）
- `scenes/HUD_Scene/ERS_CardItem.tscn`（新增）
- `scenes/HUD_Scene/ERSLayer.tscn`（优化）

---

#### [15:25] ERS 引用清理 - 删除
**需求描述**：移除项目中对 ERS 界面的脚本依赖，为后续重做 ERS 功能清空入口链路。

**开发内容**：
- 删除 `GameHUD` 中对 `ERSLayer/ERS_Manager` 的节点引用与 `open_ers()` 对外接口。
- 删除 `HomePortal` 中 ERS 查找、信号连接、商店打开逻辑，改为交互后直接进入探险场景。
- 保留传送门原有交互提示与探险参数传递流程，避免影响基础传送体验。

**开发方式**：
- 仅移除脚本层 ERS 依赖，不做场景重构，先保证运行时不再触发 ERS 相关引用错误。

**涉及文件**：
- `script/HUD/GameHUD.gd`（删除）
- `script/Object/HomePortal.gd`（删除）

---

## 📊 模块索引

| 模块名称 | 主要脚本 | 状态 | 最后更新 |
|---------|---------|------|---------|
| 敌人 AI（听觉/索敌/绕行） | `script/Character/Enemy_state/enemy.gd` | 开发中 | 2026-04-21 |
| 敌人巡逻状态 | `script/Character/Enemy_state/EnemyPatrolState.gd` | 开发中 | 2026-04-21 |
| HUD 主界面 | `script/HUD/GameHUD.gd` | 开发中 | 2026-04-21 |
| 通用合成台 UI | `script/HUD/CraftingWorkbenchUI.gd` | 新增 | 2026-05-08 |

---

## 📌 开发规范提醒

### 节点引用规范
- ✅ 内部固定节点：使用 `@onready var node: Type = $NodePath`
- ✅ 外部动态实体：使用 Group 或全局单例
- ❌ 禁止：`get_node("String")` 魔法字符串
- ❌ 禁止：跨实体微观操纵（如 `player.get_node("Stats")`）

### 注释规范
- 变量：行末 `#` 注释
- `@export` 变量：上方 `##` 文档注释 + 行末 `#` 注释
- 方法：`func` 上方 `#` 功能注释

### 代码分区
- `#region 1. 暴露的配置参数`
- `#region 2. 内部状态数据`
- `#region 3. 生命周期与核心逻辑`
- `#region 4. 辅助方法`

---

## 🔄 Trae 记录规则（重要）

### 必须记录的情况
1. **新增功能**：新增脚本、节点、系统、场景
2. **重要优化**：重构架构、性能优化、设计模式调整
3. **删除内容**：删除脚本、节点、废弃功能
4. **配置变更**：项目设置、重要参数调整
5. **接口变更**：新增/修改信号、暴露的方法、`@export` 参数

### 不需要记录的情况
1. BUG 修复（除非涉及架构调整）
2. 临时调试代码
3. 注释补充或修正
4. 微小的数值调整（如 `speed: 300.0` → `speed: 350.0`）
5. 代码格式调整（缩进、空行等）

### 记录格式模板

#### 单条记录模板
```
#### [HH:MM] 模块名称 - 操作类型（新增/优化/删除）

**需求描述**：（一句话说明本次开发的需求）

**开发内容**：
- 具体修改内容 1
- 具体修改内容 2
- （如有新增方法，简要说明方法名和功能）

**开发方式**：（可选，说明技术方案或设计思路）

**涉及文件**：
- `path/to/file.gd`（新增/修改/删除）
```

#### 每日总结模板
```
### 📅 YYYY-MM-DD 开发总结

**今日核心进展**：
- 1-2 句话概括今日主要成果

**主要修改模块**：
- 模块 A：简要说明
- 模块 B：简要说明

**待解决问题**：（可选，如有遗留问题则记录）
- 问题 1
- 问题 2
```

### 记录执行规则

1. **实时记录**：每次完成一个开发任务后，立即在「详细修改记录」区域追加一条记录
2. **时间标注**：使用 24 小时制，格式 `[HH:MM]`，如 `[14:30]`
3. **日期分组**：如果是新的一天，先创建新的日期标题（如 `### 2026-04-23`），再追加记录
4. **更新总结**：每日开发结束前，更新「最近更新」区域的每日总结
5. **更新索引**：如有新模块或模块状态变更，同步更新「模块索引」表格
6. **去重原则**：同一天内对同一模块的多次修改，可合并为一条记录，避免碎片化

### 示例记录

```
#### [14:30] 玩家移动系统 - 新增

**需求描述**：实现玩家基础移动功能，支持键盘 WASD 控制

**开发内容**：
- 新增 `Player.gd` 脚本，绑定到 Player 节点
- 实现基础移动逻辑：`_physics_process()` 中处理输入向量
- 配置参数：`base_speed = 300.0`

**开发方式**：
- 使用 `Input.get_vector()` 获取输入
- 使用 `velocity` 和 `move_and_slide()` 实现移动

**涉及文件**：
- `scripts/player/Player.gd`（新增）
```

---

## 📂 项目目录结构参考

```
project/
├── scenes/           # 场景文件
│   ├── Main.tscn
│   └── entities/
├── scripts/          # GDScript 脚本
│   ├── player/
│   ├── enemy/
│   └── components/
├── assets/           # 资源文件
│   ├── sprites/
│   └── audio/
└── project.godot     # 项目配置
```

---

**最后更新**：2026-05-08 18:16
